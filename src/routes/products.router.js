import { Router } from "express"
import { uploader } from "../utils.js"

const router = Router();

let BBDD = []

router.get('/', (req, res) => {
    req.statusCode(202).json({
        
    }
    )
})

router.post('/', )